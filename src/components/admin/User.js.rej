diff a/src/components/admin/User.js b/src/components/admin/User.js	(rejected hunks)
@@ -1 +1 @@
-import React, { useState, useEffect } from 'react'
+import React, { useState, useEffect } from 'react';
@@ -17 +17 @@ function User() {
-            'Authorization': `${token}`
+            Authorization: `${token}`
@@ -39 +39 @@ function User() {
-      })
+      });
@@ -44 +44 @@ function User() {
-          'Authorization': `${token}`
+          Authorization: `${token}`
@@ -53 +53 @@ function User() {
-  }
+  };
@@ -55 +55,2 @@ function User() {
-  const userList = users.length > 0 && (
+  const userList =
+    users.length > 0 &&
@@ -62 +63 @@ function User() {
-            <input 
+            <input
@@ -66 +67 @@ function User() {
-              onChange={(e) => toggleUserRole(user.email, user.role)} 
+              onChange={e => toggleUserRole(user.email, user.role)}
@@ -72,2 +73 @@ function User() {
-    )
-  ))
+    ));
@@ -75,17 +75,17 @@ function User() {
-  return !isError ? (isAuthorized && (
-    <>
-      <h4>User Admin Page</h4>
-      <table>
-        <thead>
-          <tr>
-            <th>Name</th>
-            <th>Email</th>
-            <th>Role (checked if admin!)</th>
-          </tr>
-        </thead>
-        <tbody>
-          { userList }
-        </tbody>
-      </table>
-    </>
-  )) : (
+  return !isError ? (
+    isAuthorized && (
+      <>
+        <h4>User Admin Page</h4>
+        <table>
+          <thead>
+            <tr>
+              <th>Name</th>
+              <th>Email</th>
+              <th>Role (checked if admin!)</th>
+            </tr>
+          </thead>
+          <tbody>{userList}</tbody>
+        </table>
+      </>
+    )
+  ) : (
@@ -93 +93 @@ function User() {
-  )
+  );
